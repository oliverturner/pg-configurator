{
  "name": "pg-configurator",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "Oliver Turner",
  "license": "ISC",
  "volta": {
    "node": "18.10.0"
  },
  "workspaces": [
    "app"
  ],
  "devDependencies": {
    "@architect/architect": "10.7.0"
  },
  "dependencies": {
    "@architect/functions": "5.2.2",
    "@begin/data": "4.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:api": "npx arc sandbox",
    "dev:app": "npm run dev -w app",
    "build:app": "npm run build -w app",
    "predeploy": "npm run build:app",
    "deploy": "npx arc deploy --static --prune"
  }
}
